<br><br><br>
<h1><strong>Category:</strong> <%= @category.title %></h1>

<p><strong> Details:</strong> <%= @category.details %></p>


<p><%= link_to "Edit", edit_category_path(@category) %>
<%= link_to "Destroy", category_path(@category),
                        method: :delete,
                        data: { confirm: "Are you sure?" } %> 
<%= link_to "Back", categories_path %>        </p>                
<br><br><br>
<%= render @category.tasks %>
<br><br><br>
<h2> Add tasks: </h2>
<%= render 'tasks/form' %>